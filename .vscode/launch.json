{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debog Mode",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/App.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "PATH",
                    "value": "${workspaceFolder}/Engine/Dependencies/GLFW;${env:PATH}"
                }
            ],
            "externalConsole": false
        }
    ]
}